apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: users
  namespace: NAMESPACE_PLACEHOLDER
spec:
  hosts:
  - "*"
  gateways:
  - aks-istio-ingress/store-ui-gateway
  http:
  - match:
    - uri:
        prefix: /api/users
    rewrite:
      uri: /
    route:
    - destination:
        host: users-service.NAMESPACE_PLACEHOLDER.svc.cluster.local
        port:
          number: 9090