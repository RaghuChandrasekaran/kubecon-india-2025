apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: search
  namespace: NAMESPACE_PLACEHOLDER
spec:
  hosts:
  - "*"
  gateways:
  - aks-istio-ingress/store-ui-gateway
  http:
  - match:
    - uri:
        prefix: /api/search
    rewrite:
      uri: /
    route:
    - destination:
        host: search-service.NAMESPACE_PLACEHOLDER.svc.cluster.local
        port:
          number: 4000