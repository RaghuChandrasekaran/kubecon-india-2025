apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: cart
  namespace: NAMESPACE_PLACEHOLDER
spec:
  hosts:
  - "*"
  gateways:
  - aks-istio-ingress/store-ui-gateway
  http:
  - match:
    - uri:
        prefix: /api/cart
    rewrite:
      uri: /
    route:
    - destination:
        host: cart-service.NAMESPACE_PLACEHOLDER.svc.cluster.local
        port:
          number: 7000